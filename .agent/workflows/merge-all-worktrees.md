---
description: Merge changes from all existing worktrees
---
1. Check for worktrees using `git worktree list`.
2. Identify branches checked out in other worktrees.
3. Merge those branches into the current branch using `git merge [branch_name]`.
